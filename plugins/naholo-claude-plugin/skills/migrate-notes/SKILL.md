---
name: migrate-notes
description: 'Migrate the infilled issue notes from the old contract to the new one: rename OPERATION to PLAN and RECON* to DRAFT*, rewrite old headings, delete the stale server notes.'
argument-hint: ''
---

Run `naholo agent skills migrate-notes` and follow stdout.

If `naholo` is not found or the command errors, tell the user to run `/naholo-doctor` to diagnose and fix the CLI setup, then stop.
