---
name: splash-out
description: Close out a reviewed splash — commit the staged task changes under the task title, push tasks + notes, and enter the opord phase.
argument-hint: '["freeform"]'
---

Run `naholo agent skills splash-out --nightly` and follow stdout.

If `naholo` is not found or the command errors, tell the user to run `/naholo-doctor` to diagnose and fix the CLI setup, then stop.
