---
name: epic-plan
description: 'Write or revise the epic-level plan: writes notes/README.md (SYNOPSIS / ADR / ISSUES) from the epic prep bench, marking issues that do not exist yet with `#?` for /epic-issues to spawn.'
argument-hint: '["freeform"]'
---

Run `naholo agent skills epic-plan --nightly` and follow stdout.

If `naholo` is not found or the command errors, tell the user to run `/naholo-doctor` to diagnose and fix the CLI setup, then stop.
